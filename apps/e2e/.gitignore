.artifacts/
playwright-report/
test-results/

# Regenerated by `yarn codegen`, which every entry point runs first. Not
# committed: nothing outside this workspace imports it, so there is no consumer
# to protect from staleness and therefore no drift check to maintain.
src/generated/
